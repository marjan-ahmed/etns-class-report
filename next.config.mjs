/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.js
    images: {
      domains: ['lh3.googleusercontent.com', 'static.wixstatic.com'],// Add Google image domain here
    },
  };    

export default nextConfig;
